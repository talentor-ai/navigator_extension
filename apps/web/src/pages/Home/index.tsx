import { Typography } from 'antd';

const Home = () => {
  return (
    <div>
      <Typography.Title level={1}>Talentor</Typography.Title>
      <Typography.Paragraph>
        Web platform foundation. Authentication and profile management arrive in
        the next slices.
      </Typography.Paragraph>
    </div>
  );
};

export default Home;
